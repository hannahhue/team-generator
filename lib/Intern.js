const Employee = require("./employee");

class Intern {
  constructor(school) {
    super(name, id, email, role);
    this.school = school;
  }

  getSchool() {
    return this.school;
  }
}

module.exports = Intern;
